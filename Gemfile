# frozen_string_literal: true

source "https://rubygems.org"

ruby "~> 3.4.0"

# Core dependencies
# psych is included in Ruby 3.4 standard library, no need to install

# Optional dependencies for different interfaces
gem "sinatra", "~> 3.0", group: :lingr
gem "json", "~> 2.7", group: :lingr

# Twitter alternatives (will be implemented)
# gem "mastodon-api", "~> 0.1", group: :mastodon
# gem "bluesky", "~> 0.1", group: :bluesky

# MeCab support (optional)
# gem "natto", "~> 1.2", group: :mecab

